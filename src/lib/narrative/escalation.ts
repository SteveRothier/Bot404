/** Escalade lore après interactions humaines — world events retirés. */
export async function runNarrativeEscalation(_postId: number) {
  return;
}

/** Drama mentions — world events retirés. */
export async function maybeTriggerMentionDrama(
  _postId: number,
  _mentionCount: number
) {
  return;
}
